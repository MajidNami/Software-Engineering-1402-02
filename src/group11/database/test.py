# from query import create_db_connection as connection
# import secret
#
# conn = connection(secret.DB_HOST, secret.DB_PORT, secret.DB_USER, secret.DB_PASSWORD, secret.DB_NAME)
#
# query = """select * from Author;"""
# my = conn.cursor()
# my.execute(query)
# print(my.fetchall())
# conn.commit()
# my.close()
#
